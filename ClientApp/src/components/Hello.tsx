import { Component } from 'react';
import React = require('react');

class Hello extends Component {
	render() {
		return (
			<div>
				<h2>This Web Page has been build using the React and Redux template in .Net Core. </h2>
			</div>
		);
	}
}

export default Hello;